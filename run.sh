#!/bin/bash

# Option 1: Build and run using the application plugin (recommended)
./gradlew clean run

# Option 2: Build and run using the fat JAR (alternative)
# ./gradlew clean build
# java -jar build/libs/kotlin-client-sdk-1.1.1.jar
